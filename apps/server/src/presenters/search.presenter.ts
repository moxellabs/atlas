/** Presents scope inference output without changing retrieval semantics. */
export function presentScopes(result: unknown) {
  return result;
}

/** Presents ranked document search output without changing retrieval semantics. */
export function presentSearch(result: unknown) {
  return result;
}
